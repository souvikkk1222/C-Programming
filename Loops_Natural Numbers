#include <stdio.h>

int main() {
    int N;

    scanf("%d", &N);
    if (N >= 0 && N <= 1000000) {
        for (int i = 1; i <= N; i++) {
            printf("%d ", i);
        }
        printf("\n"); 
    }

    return 0;
}
